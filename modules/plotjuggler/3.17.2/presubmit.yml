matrix:
  platform:
  - ubuntu2004
  bazel:
  - 7.x
  - 8.x
  - rolling
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@plotjuggler//plotjuggler_base:plotjuggler_base'
    - '@plotjuggler//3rdparty/qwt:plotjuggler_qwt'
